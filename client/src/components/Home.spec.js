import {mount} from '@vue/test-utils'
import Home from './Home.vue'

describe ('Home.vue',()=>
{
    it('Check to see if calculate button works', () => 
    {
        expect(true).toBe(true)
    })
})