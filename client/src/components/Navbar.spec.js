//import {mount} from '@vue/test-utils'
//import Navbar from './Navbar.vue'
describe ('Navbar.vue',()=>
{
    test('Navbar opens new expected page when pressed', () => 
    {
        expect (true).toBe(true)
       // const wrapper = mount(Navbar)
        //expect(wrapper.text()).toContain
    })
})